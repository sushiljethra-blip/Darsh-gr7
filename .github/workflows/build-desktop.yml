name: Build Windows EXE

on:
  push:
    branches:
      - claude/sip-phone-dialer-auto-dial-OzKmr
    paths:
      - 'sip-dialer-app/**'
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        shell: bash
        run: |
          python -m pip install --upgrade pip
          # PyAudio now ships Windows wheels on PyPI (no pipwin needed)
          pip install pyaudio || echo "PyAudio unavailable — audio will be disabled"
          pip install "pyvoip>=1.6.4" "numpy>=1.24.0" pyinstaller

      - name: Build EXE
        shell: bash
        working-directory: sip-dialer-app
        run: |
          python -m PyInstaller \
            --onefile \
            --windowed \
            --name "BPO-SIP-Dialer" \
            --hidden-import pyvoip \
            --hidden-import pyvoip.voip \
            --hidden-import pyvoip.call \
            --hidden-import pyvoip.rtp \
            --hidden-import pyvoip.sip \
            --hidden-import pyaudio \
            --hidden-import numpy \
            --collect-all pyvoip \
            main.py

      - name: Upload EXE
        uses: actions/upload-artifact@v4
        with:
          name: BPO-SIP-Dialer-Windows
          path: sip-dialer-app/dist/BPO-SIP-Dialer.exe
          retention-days: 30
